backend:
  image:
    tag: "1.0.35"

frontend:
  image:
    tag: "1.0.35"
  razorpayKeyId: "rzp_test_TOpPJJoN5F2yHB"

secrets:
  azure:
    keyvaultName: "shopnest-dev-kv2"

ingress:
  # ingress-nginx's LoadBalancer external IP, via the free nip.io wildcard
  # DNS trick (no real domain owned yet). Update this if the IP ever changes
  # (e.g. ingress-nginx gets reinstalled).
  host: "48.201.27.112.nip.io"
  tls:
    enabled: true
